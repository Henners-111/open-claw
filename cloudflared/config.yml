# cloudflared/config.yml (template - edit with your tunnel ID and credentials file)
tunnel: <YOUR_TUNNEL_ID>
credentials-file: /home/henry/.cloudflared/<YOUR_TUNNEL_ID>.json

ingress:
  - hostname: openclaw.example.com
    service: http://127.0.0.1:8080
  - service: http_status:404
